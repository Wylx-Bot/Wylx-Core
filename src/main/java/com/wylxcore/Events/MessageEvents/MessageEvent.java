package com.wylxcore.Events.MessageEvents;

public class MessageEvent {
    // Events that happen as a result of the user sending a text message
    // Offers two versions
    // Version A: event occurs as a result of any regex expression
    // Version B: event occurs when there is a server prefix followed by the events name, rest of the message is passed to the event
}
