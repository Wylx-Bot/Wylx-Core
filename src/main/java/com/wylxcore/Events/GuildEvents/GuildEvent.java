package com.wylxcore.Events.GuildEvents;

public class GuildEvent {
    // Potentially not needed, idea is to offer a simple framework that provides an extremely barebones framework for responding to guild events
    // Potential uses: New thread, Member join, Moderation Logging (kicked / banned / unbanned / message deleted)
}
